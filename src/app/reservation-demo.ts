export class ReservationDemo {
    reservationId: string;
    reservationName: string;
    reservationContactNumber: number;
    reservationCapacity: number;
    reservationDate: string;
    constructor(reservationId, reservationName, reservationContactNumber, reservationCapacity, reservationDate){
        reservationId = reservationId;
        reservationName = reservationName;
        reservationContactNumber = reservationContactNumber;
        reservationCapacity = reservationCapacity;
        reservationDate = reservationDate;
    }
}
